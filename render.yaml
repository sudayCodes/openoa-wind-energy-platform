services:
  - type: web
    name: openoa-platform
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: PYTHONPATH
        value: /app/backend
      - key: OPENOA_ROOT
        value: /app/OpenOA
